<%= form_for(user) do |f| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :ort %>
    <%= f.text_field :ort %>
  </div>

  <div class="field">
    <%= f.label :meetup_id %>
    <%= collection_select(:user, :meetup_id,
    Meetup.all,
    :id,
    :art,
    prompt: true) %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>

<% end %>
